package pl.pawelkielb.xchat.server.managers

import pl.pawelkielb.xchat.server.defaultPageSize
import java.util.*

class MessageManager {
    fun list(channel: UUID, page: Int = 0, pageSize: Int = defaultPageSize) {
        
    }
}
